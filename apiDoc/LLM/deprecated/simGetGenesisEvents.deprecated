name: sim.getGenesisEvents
description: Retrieves all events that allow to reconstruct a scene's (mostly) visual content remotely
luaSynopsis: bytes events = sim.getGenesisEvents()
pythonSynopsis: buffer events = sim.getGenesisEvents()
inputs:
outputs:
1. events: the genesis events as a cbor-coded buffer
