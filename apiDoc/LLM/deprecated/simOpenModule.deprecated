name: sim.openModule
description: 
luaSynopsis: (1) int result=sim.openModule(int sim.handle_all)
inputs (possibly taken/adapted from C-function counterpart. Adjust accordingly):
1. sim.handle_all: indicates that all plugins should be opened
2. moduleName: the name of the plugin that should be opened
outputs (possibly taken/adapted from C-function counterpart. Adjust accordingly):
1. result: the number of plugins that executed the command.
