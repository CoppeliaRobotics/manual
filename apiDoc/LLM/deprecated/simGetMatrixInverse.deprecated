name: sim.getMatrixInverse
description: Inverts a transformation matrix
luaSynopsis: list matrix = sim.getMatrixInverse(list matrix)
pythonSynopsis: float[12] matrix = sim.getMatrixInverse(float[12] matrix)
inputs:
1. matrix: the input matrix
outputs:
1. matrix: the output matrix
replacedBy:
- use simEigen methods