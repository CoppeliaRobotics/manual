name: sim.getObjectSel
description: Retrieves the handles of selected objects
luaSynopsis: list selectedObjects = sim.getObjectSel()
pythonSynopsis: int[] selectedObjects = sim.getObjectSel()
inputs:
outputs:
1. selectedObjects: the handles of selected objects
