name: sim.handleSensingStart
description: Handles various functionality (e.g. camera tracking during simulation, object velocity calculation, etc.). Should only be called from the main script, as the first instruction in the sensing section
luaSynopsis: sim.handleSensingStart()
pythonSynopsis: sim.handleSensingStart()
inputs:
outputs:
