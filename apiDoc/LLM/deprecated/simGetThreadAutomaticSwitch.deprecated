name: sim.getThreadAutomaticSwitch
description: Deprecated
luaSynopsis: bool result,int lockLevel=sim.getThreadAutomaticSwitch()
inputs (possibly taken/adapted from C-function counterpart. Adjust accordingly):
outputs (possibly taken/adapted from C-function counterpart. Adjust accordingly):
1. result: if true, the thread will automatically be interrupted, i.e. switched
2. lockLevel: the current thread switch lock level
