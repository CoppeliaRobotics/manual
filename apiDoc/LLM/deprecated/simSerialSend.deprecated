name: sim.serialSend
description: Writes data to a previously opened serial port (RS-232)
pythonSynopsis: int charsSent = sim.serialSend(int portHandle, buffer data)
inputs:
1. portHandle: handle returned by the sim.serialOpen function
2. data: pointer to the data that should be sent
outputs:
1. charsSent: the effective data length that was written
