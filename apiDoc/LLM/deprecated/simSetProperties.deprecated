name: sim.setProperties
description: Convenience function to set several properties at once
luaSynopsis: sim.setProperties(int target, dict properties)
pythonSynopsis: sim.setProperties(int target, map properties)
inputs:
1. target: target item
2. properties: a map where keys are propertyNames and values are propertyStates
outputs:
