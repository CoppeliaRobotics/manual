name: sim.getRealTimeSimulation
description: Deprecated. Use sim.getBoolProperty(sim.handle_scene, 'realtimeSimulation') instead
luaSynopsis: int result = sim.getRealTimeSimulation()
pythonSynopsis: int result = sim.getRealTimeSimulation()
inputs:
outputs:
1. result: 1 if simulation is real-time, or 0 if it is not
