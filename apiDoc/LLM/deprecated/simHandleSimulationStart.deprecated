name: sim.handleSimulationStart
description: Initializes various functionality (e.g. camera tracking during simulation, object velocity calculation, etc.). Should only be called from the main script, as the first instruction in the initialization section
luaSynopsis: sim.handleSimulationStart()
pythonSynopsis: sim.handleSimulationStart()
inputs:
outputs:
