name: sim.getJointMode
description: Retrieves the operation mode of a joint
luaSynopsis: int jointMode = sim.getJointMode(int jointHandle)
pythonSynopsis: int jointMode = sim.getJointMode(int jointHandle)
inputs:
1. jointHandle: handle of the joint object
outputs:
1. jointMode: the joint mode
